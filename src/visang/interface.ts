export enum RECORD_PROG {
    INIT,
    WAIT_RECORD_START,
    RECORDING,
    WAIT_RECORDED,
    RECORDED
  } /* 예시로 사용하는 enum 입니다 실제로 사용할때는 각 스토어에 맞게 사용하세요 */
  export enum SEND_PROG {
    HIDE,
    SHOW,
    SENDING,
    SENDED
  }